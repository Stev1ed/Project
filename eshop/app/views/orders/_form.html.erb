<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :OrderID %>
    <%= form.number_field :OrderID, id: :order_OrderID %>
  </div>

  <div class="field">
    <%= form.label :CustomerID %>
    <%= form.number_field :CustomerID, id: :order_CustomerID %>
  </div>

  <div class="field">
    <%= form.label :OrderDate %>
    <%= form.date_select :OrderDate, id: :order_OrderDate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
